// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Youtube is an object representing the database table.
type Youtube struct {
	Date        time.Time `boil:"date" json:"date" toml:"date" yaml:"date"`
	Subscribers int       `boil:"subscribers" json:"subscribers" toml:"subscribers" yaml:"subscribers"`
	ViewCount   int       `boil:"view_count" json:"view_count" toml:"view_count" yaml:"view_count"`
	Channel     string    `boil:"channel" json:"channel" toml:"channel" yaml:"channel"`

	R *youtubeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L youtubeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var YoutubeColumns = struct {
	Date        string
	Subscribers string
	ViewCount   string
	Channel     string
}{
	Date:        "date",
	Subscribers: "subscribers",
	ViewCount:   "view_count",
	Channel:     "channel",
}

// Generated where

var YoutubeWhere = struct {
	Date        whereHelpertime_Time
	Subscribers whereHelperint
	ViewCount   whereHelperint
	Channel     whereHelperstring
}{
	Date:        whereHelpertime_Time{field: "\"youtube\".\"date\""},
	Subscribers: whereHelperint{field: "\"youtube\".\"subscribers\""},
	ViewCount:   whereHelperint{field: "\"youtube\".\"view_count\""},
	Channel:     whereHelperstring{field: "\"youtube\".\"channel\""},
}

// YoutubeRels is where relationship names are stored.
var YoutubeRels = struct {
}{}

// youtubeR is where relationships are stored.
type youtubeR struct {
}

// NewStruct creates a new relationship struct
func (*youtubeR) NewStruct() *youtubeR {
	return &youtubeR{}
}

// youtubeL is where Load methods for each relationship are stored.
type youtubeL struct{}

var (
	youtubeAllColumns            = []string{"date", "subscribers", "view_count", "channel"}
	youtubeColumnsWithoutDefault = []string{"date", "subscribers", "view_count", "channel"}
	youtubeColumnsWithDefault    = []string{}
	youtubePrimaryKeyColumns     = []string{"date"}
)

type (
	// YoutubeSlice is an alias for a slice of pointers to Youtube.
	// This should generally be used opposed to []Youtube.
	YoutubeSlice []*Youtube

	youtubeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	youtubeType                 = reflect.TypeOf(&Youtube{})
	youtubeMapping              = queries.MakeStructMapping(youtubeType)
	youtubePrimaryKeyMapping, _ = queries.BindMapping(youtubeType, youtubeMapping, youtubePrimaryKeyColumns)
	youtubeInsertCacheMut       sync.RWMutex
	youtubeInsertCache          = make(map[string]insertCache)
	youtubeUpdateCacheMut       sync.RWMutex
	youtubeUpdateCache          = make(map[string]updateCache)
	youtubeUpsertCacheMut       sync.RWMutex
	youtubeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single youtube record from the query.
func (q youtubeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Youtube, error) {
	o := &Youtube{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for youtube")
	}

	return o, nil
}

// All returns all Youtube records from the query.
func (q youtubeQuery) All(ctx context.Context, exec boil.ContextExecutor) (YoutubeSlice, error) {
	var o []*Youtube

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to Youtube slice")
	}

	return o, nil
}

// Count returns the count of all Youtube records in the query.
func (q youtubeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count youtube rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q youtubeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if youtube exists")
	}

	return count > 0, nil
}

// Youtubes retrieves all the records using an executor.
func Youtubes(mods ...qm.QueryMod) youtubeQuery {
	mods = append(mods, qm.From("\"youtube\""))
	return youtubeQuery{NewQuery(mods...)}
}

// FindYoutube retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindYoutube(ctx context.Context, exec boil.ContextExecutor, date time.Time, selectCols ...string) (*Youtube, error) {
	youtubeObj := &Youtube{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"youtube\" where \"date\"=$1", sel,
	)

	q := queries.Raw(query, date)

	err := q.Bind(ctx, exec, youtubeObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from youtube")
	}

	return youtubeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Youtube) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no youtube provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(youtubeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	youtubeInsertCacheMut.RLock()
	cache, cached := youtubeInsertCache[key]
	youtubeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			youtubeAllColumns,
			youtubeColumnsWithDefault,
			youtubeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(youtubeType, youtubeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(youtubeType, youtubeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"youtube\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"youtube\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into youtube")
	}

	if !cached {
		youtubeInsertCacheMut.Lock()
		youtubeInsertCache[key] = cache
		youtubeInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the Youtube.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Youtube) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	youtubeUpdateCacheMut.RLock()
	cache, cached := youtubeUpdateCache[key]
	youtubeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			youtubeAllColumns,
			youtubePrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update youtube, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"youtube\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, youtubePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(youtubeType, youtubeMapping, append(wl, youtubePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update youtube row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for youtube")
	}

	if !cached {
		youtubeUpdateCacheMut.Lock()
		youtubeUpdateCache[key] = cache
		youtubeUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q youtubeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for youtube")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for youtube")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o YoutubeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), youtubePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"youtube\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, youtubePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in youtube slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all youtube")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Youtube) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no youtube provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(youtubeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	youtubeUpsertCacheMut.RLock()
	cache, cached := youtubeUpsertCache[key]
	youtubeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			youtubeAllColumns,
			youtubeColumnsWithDefault,
			youtubeColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			youtubeAllColumns,
			youtubePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert youtube, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(youtubePrimaryKeyColumns))
			copy(conflict, youtubePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"youtube\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(youtubeType, youtubeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(youtubeType, youtubeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert youtube")
	}

	if !cached {
		youtubeUpsertCacheMut.Lock()
		youtubeUpsertCache[key] = cache
		youtubeUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single Youtube record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Youtube) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no Youtube provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), youtubePrimaryKeyMapping)
	sql := "DELETE FROM \"youtube\" WHERE \"date\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from youtube")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for youtube")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q youtubeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no youtubeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from youtube")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for youtube")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o YoutubeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), youtubePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"youtube\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, youtubePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from youtube slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for youtube")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Youtube) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindYoutube(ctx, exec, o.Date)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *YoutubeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := YoutubeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), youtubePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"youtube\".* FROM \"youtube\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, youtubePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in YoutubeSlice")
	}

	*o = slice

	return nil
}

// YoutubeExists checks if the Youtube row exists.
func YoutubeExists(ctx context.Context, exec boil.ContextExecutor, date time.Time) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"youtube\" where \"date\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, date)
	}
	row := exec.QueryRowContext(ctx, sql, date)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if youtube exists")
	}

	return exists, nil
}
