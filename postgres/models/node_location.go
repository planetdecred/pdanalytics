// Code generated by SQLBoiler 3.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// NodeLocation is an object representing the database table.
type NodeLocation struct {
	Timestamp int64  `boil:"timestamp" json:"timestamp" toml:"timestamp" yaml:"timestamp"`
	Height    int64  `boil:"height" json:"height" toml:"height" yaml:"height"`
	NodeCount int    `boil:"node_count" json:"node_count" toml:"node_count" yaml:"node_count"`
	Country   string `boil:"country" json:"country" toml:"country" yaml:"country"`
	Bin       string `boil:"bin" json:"bin" toml:"bin" yaml:"bin"`

	R *nodeLocationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L nodeLocationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var NodeLocationColumns = struct {
	Timestamp string
	Height    string
	NodeCount string
	Country   string
	Bin       string
}{
	Timestamp: "timestamp",
	Height:    "height",
	NodeCount: "node_count",
	Country:   "country",
	Bin:       "bin",
}

// Generated where

var NodeLocationWhere = struct {
	Timestamp whereHelperint64
	Height    whereHelperint64
	NodeCount whereHelperint
	Country   whereHelperstring
	Bin       whereHelperstring
}{
	Timestamp: whereHelperint64{field: "\"node_location\".\"timestamp\""},
	Height:    whereHelperint64{field: "\"node_location\".\"height\""},
	NodeCount: whereHelperint{field: "\"node_location\".\"node_count\""},
	Country:   whereHelperstring{field: "\"node_location\".\"country\""},
	Bin:       whereHelperstring{field: "\"node_location\".\"bin\""},
}

// NodeLocationRels is where relationship names are stored.
var NodeLocationRels = struct {
}{}

// nodeLocationR is where relationships are stored.
type nodeLocationR struct {
}

// NewStruct creates a new relationship struct
func (*nodeLocationR) NewStruct() *nodeLocationR {
	return &nodeLocationR{}
}

// nodeLocationL is where Load methods for each relationship are stored.
type nodeLocationL struct{}

var (
	nodeLocationAllColumns            = []string{"timestamp", "height", "node_count", "country", "bin"}
	nodeLocationColumnsWithoutDefault = []string{"timestamp", "height", "node_count", "country"}
	nodeLocationColumnsWithDefault    = []string{"bin"}
	nodeLocationPrimaryKeyColumns     = []string{"timestamp", "bin", "country"}
)

type (
	// NodeLocationSlice is an alias for a slice of pointers to NodeLocation.
	// This should generally be used opposed to []NodeLocation.
	NodeLocationSlice []*NodeLocation

	nodeLocationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	nodeLocationType                 = reflect.TypeOf(&NodeLocation{})
	nodeLocationMapping              = queries.MakeStructMapping(nodeLocationType)
	nodeLocationPrimaryKeyMapping, _ = queries.BindMapping(nodeLocationType, nodeLocationMapping, nodeLocationPrimaryKeyColumns)
	nodeLocationInsertCacheMut       sync.RWMutex
	nodeLocationInsertCache          = make(map[string]insertCache)
	nodeLocationUpdateCacheMut       sync.RWMutex
	nodeLocationUpdateCache          = make(map[string]updateCache)
	nodeLocationUpsertCacheMut       sync.RWMutex
	nodeLocationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single nodeLocation record from the query.
func (q nodeLocationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*NodeLocation, error) {
	o := &NodeLocation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for node_location")
	}

	return o, nil
}

// All returns all NodeLocation records from the query.
func (q nodeLocationQuery) All(ctx context.Context, exec boil.ContextExecutor) (NodeLocationSlice, error) {
	var o []*NodeLocation

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to NodeLocation slice")
	}

	return o, nil
}

// Count returns the count of all NodeLocation records in the query.
func (q nodeLocationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count node_location rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q nodeLocationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if node_location exists")
	}

	return count > 0, nil
}

// NodeLocations retrieves all the records using an executor.
func NodeLocations(mods ...qm.QueryMod) nodeLocationQuery {
	mods = append(mods, qm.From("\"node_location\""))
	return nodeLocationQuery{NewQuery(mods...)}
}

// FindNodeLocation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindNodeLocation(ctx context.Context, exec boil.ContextExecutor, timestamp int64, bin string, country string, selectCols ...string) (*NodeLocation, error) {
	nodeLocationObj := &NodeLocation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"node_location\" where \"timestamp\"=$1 AND \"bin\"=$2 AND \"country\"=$3", sel,
	)

	q := queries.Raw(query, timestamp, bin, country)

	err := q.Bind(ctx, exec, nodeLocationObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from node_location")
	}

	return nodeLocationObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *NodeLocation) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no node_location provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(nodeLocationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	nodeLocationInsertCacheMut.RLock()
	cache, cached := nodeLocationInsertCache[key]
	nodeLocationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			nodeLocationAllColumns,
			nodeLocationColumnsWithDefault,
			nodeLocationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(nodeLocationType, nodeLocationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(nodeLocationType, nodeLocationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"node_location\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"node_location\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into node_location")
	}

	if !cached {
		nodeLocationInsertCacheMut.Lock()
		nodeLocationInsertCache[key] = cache
		nodeLocationInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the NodeLocation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *NodeLocation) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	nodeLocationUpdateCacheMut.RLock()
	cache, cached := nodeLocationUpdateCache[key]
	nodeLocationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			nodeLocationAllColumns,
			nodeLocationPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update node_location, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"node_location\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, nodeLocationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(nodeLocationType, nodeLocationMapping, append(wl, nodeLocationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update node_location row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for node_location")
	}

	if !cached {
		nodeLocationUpdateCacheMut.Lock()
		nodeLocationUpdateCache[key] = cache
		nodeLocationUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q nodeLocationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for node_location")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for node_location")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o NodeLocationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), nodeLocationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"node_location\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, nodeLocationPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in nodeLocation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all nodeLocation")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *NodeLocation) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no node_location provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(nodeLocationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	nodeLocationUpsertCacheMut.RLock()
	cache, cached := nodeLocationUpsertCache[key]
	nodeLocationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			nodeLocationAllColumns,
			nodeLocationColumnsWithDefault,
			nodeLocationColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			nodeLocationAllColumns,
			nodeLocationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert node_location, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(nodeLocationPrimaryKeyColumns))
			copy(conflict, nodeLocationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"node_location\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(nodeLocationType, nodeLocationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(nodeLocationType, nodeLocationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert node_location")
	}

	if !cached {
		nodeLocationUpsertCacheMut.Lock()
		nodeLocationUpsertCache[key] = cache
		nodeLocationUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single NodeLocation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *NodeLocation) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no NodeLocation provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), nodeLocationPrimaryKeyMapping)
	sql := "DELETE FROM \"node_location\" WHERE \"timestamp\"=$1 AND \"bin\"=$2 AND \"country\"=$3"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from node_location")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for node_location")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q nodeLocationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no nodeLocationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from node_location")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for node_location")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o NodeLocationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), nodeLocationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"node_location\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, nodeLocationPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from nodeLocation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for node_location")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *NodeLocation) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindNodeLocation(ctx, exec, o.Timestamp, o.Bin, o.Country)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *NodeLocationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := NodeLocationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), nodeLocationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"node_location\".* FROM \"node_location\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, nodeLocationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in NodeLocationSlice")
	}

	*o = slice

	return nil
}

// NodeLocationExists checks if the NodeLocation row exists.
func NodeLocationExists(ctx context.Context, exec boil.ContextExecutor, timestamp int64, bin string, country string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"node_location\" where \"timestamp\"=$1 AND \"bin\"=$2 AND \"country\"=$3 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, timestamp, bin, country)
	}
	row := exec.QueryRowContext(ctx, sql, timestamp, bin, country)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if node_location exists")
	}

	return exists, nil
}
