// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testTwitters(t *testing.T) {
	t.Parallel()

	query := Twitters()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testTwittersDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTwittersQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Twitters().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTwittersSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TwitterSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTwittersExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := TwitterExists(ctx, tx, o.Date)
	if err != nil {
		t.Errorf("Unable to check if Twitter exists: %s", err)
	}
	if !e {
		t.Errorf("Expected TwitterExists to return true, but got false.")
	}
}

func testTwittersFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	twitterFound, err := FindTwitter(ctx, tx, o.Date)
	if err != nil {
		t.Error(err)
	}

	if twitterFound == nil {
		t.Error("want a record, got nil")
	}
}

func testTwittersBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Twitters().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testTwittersOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Twitters().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testTwittersAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	twitterOne := &Twitter{}
	twitterTwo := &Twitter{}
	if err = randomize.Struct(seed, twitterOne, twitterDBTypes, false, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}
	if err = randomize.Struct(seed, twitterTwo, twitterDBTypes, false, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = twitterOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = twitterTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Twitters().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testTwittersCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	twitterOne := &Twitter{}
	twitterTwo := &Twitter{}
	if err = randomize.Struct(seed, twitterOne, twitterDBTypes, false, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}
	if err = randomize.Struct(seed, twitterTwo, twitterDBTypes, false, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = twitterOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = twitterTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testTwittersInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTwittersInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(twitterColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTwittersReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTwittersReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TwitterSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTwittersSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Twitters().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	twitterDBTypes = map[string]string{`Date`: `timestamp without time zone`, `Handle`: `character varying`, `Followers`: `integer`}
	_              = bytes.MinRead
)

func testTwittersUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(twitterPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(twitterAllColumns) == len(twitterPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testTwittersSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(twitterAllColumns) == len(twitterPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Twitter{}
	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, twitterDBTypes, true, twitterPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(twitterAllColumns, twitterPrimaryKeyColumns) {
		fields = twitterAllColumns
	} else {
		fields = strmangle.SetComplement(
			twitterAllColumns,
			twitterPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := TwitterSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testTwittersUpsert(t *testing.T) {
	t.Parallel()

	if len(twitterAllColumns) == len(twitterPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Twitter{}
	if err = randomize.Struct(seed, &o, twitterDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Twitter: %s", err)
	}

	count, err := Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, twitterDBTypes, false, twitterPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Twitter struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Twitter: %s", err)
	}

	count, err = Twitters().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
